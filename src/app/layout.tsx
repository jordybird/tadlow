import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'Mustard Seed Marketing',
  description: 'Mustard Seed Marketing creates unforgettable experiences through innovative brand activations, dynamic sports marketing, and expertly crafted events. We partner with clients to achieve big results, connecting their brands to audiences in meaningful ways.',
  icons: ['/Mustard%20Seed%20Plant%20(1).svg'],
};
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
